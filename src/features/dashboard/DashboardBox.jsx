import styled from "styled-components";
import { motion } from "framer-motion";

const DashboardBox = styled(motion.div)`
  padding: 3.2rem;

  display: flex;
  flex-direction: column;
  gap: 2.4rem;
`;

export default DashboardBox;
